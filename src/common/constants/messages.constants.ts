export enum CommonMessage {
  GET_ITEM_BY_ID_SUCCESS = 'Get item by ID success',
  NOT_FOUND_BY_ID = 'No item found with given ID',
  ALREADY_EXIST = 'is already exist',
  UPDATE_SUCCESS = 'Item updated',
}
