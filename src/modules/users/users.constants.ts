export enum UsersMessage {
  NOT_FOUND_EMAIL = 'No user with given email.',
}

export enum UsersSummary {
  GET_BY_ID = 'Get user by ID.',
  UPDATE_BY_ID = 'Update user by ID.',
  GET_ALL = 'Get all users.',
}

export enum UsersDescription {}
